import {
    LoadingContainer,
    LoadingText
} from "../../styles/components/loadingScreen.styles";

const LoadingScreen = () => {
    return (
        <LoadingContainer>
            <LoadingText>Cargando...</LoadingText>
        </LoadingContainer>
    )
};

export default LoadingScreen;